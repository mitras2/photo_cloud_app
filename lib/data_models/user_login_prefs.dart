class UserLoginPrefs {
  final String serverAddress;
  final String username;
  final String userId;
  final String token;
  final String tokenExpires;

  UserLoginPrefs(this.serverAddress, this.username, this.userId, this.token, this.tokenExpires);

}